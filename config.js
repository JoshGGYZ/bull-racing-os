/*
 * Configuracao publica do Bull Racing OS.
 *
 * A URL e a chave anon/publishable do Supabase foram feitas para uso no
 * navegador e podem ficar neste arquivo. Nunca coloque service_role aqui.
 * Preencha estes dois campos depois de criar o projeto no Supabase.
 */
window.BULL_CONFIG = Object.freeze({
    supabaseUrl: '',
    supabaseAnonKey: '',
    teamSlug: 'bull-racing',
    syncDebounceMs: 900,
    remotePollMs: 30000
});
